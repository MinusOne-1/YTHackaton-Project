using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Multidialog : MonoBehaviour
{
    // Start is called before the first frame update
	int ch = 0;
    void Start()
    {
		
        
    }
	
	public void ch_plus(){
		ch +=0;
		
	}
    // Update is called once per frame
    void Update()
    {
        if(ch == 0){
			
		}
    }
}
